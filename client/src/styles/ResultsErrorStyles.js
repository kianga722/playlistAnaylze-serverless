import styled from 'styled-components';

const ResultsErrorStyles = styled.section`
  margin-top: 2rem;
  font-size: 2rem;
  font-weight: 700;
  color: ${props => props.theme.pink};
  text-align: center;
`;

export default ResultsErrorStyles;